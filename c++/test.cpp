#include <map>
#include <unordered_map>
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

int main() {

   // hvernig á að fylla vigur af staðalintaki?
    string s;
    vector<string> words;
    while (getline(cin, s)) {
        words.push_back(s);
    }

    // hvernig á að stokka?
    random_shuffle(shuffledwords.begin(), shuffledwords.end());

    //hvernig á að búa til klukku?
    clock_t c;
    c = clock(); // c er núna klukkupúls
    .
    . gera ehv ✨✨✨
    .
    clock() - c // gefur klukkupúlsa sem eru liðnir!

    
    viljið nota map<string, int> og unordered_map<string, int>
    instetning uom[shuffledwords[i]]=i; t.d.
    og gera innsetningu og uppfléttingu.

    return 0;
}